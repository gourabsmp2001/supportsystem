# Support System Database

Run `schema.sql` in the Supabase SQL editor before starting the app.

The app uses Supabase Auth directly from the Vercel-hosted frontend. Row Level Security is enabled on every table, and authenticated users can read, insert, update, and delete records.
